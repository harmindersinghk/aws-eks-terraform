apiVersion: apps/v1
kind: Deployment
metadata:
  name: adservice
  namespace: app
spec:
  template:
    spec:
      containers:
      - name: server
        securityContext:
          readOnlyRootFilesystem: false
